<%= title [l(:global_issue_templates), global_issue_templates_path], l(:button_add) %>

<%= labelled_form_for :template,  @template,
                      url: global_issue_templates_path,
                      html: { id: 'global_issue_template-form' } do |f| %>
  <%= render 'issue_templates/form', { f: f, global: true } %>
  <%= render partial: 'global_issue_templates/projects_form' %>

  <%= submit_tag l :button_create %>
<% end %>


